# Agent entrypoint template guidance

When used through DEEF setup, install the [managed baseline](../../managed-instructions.md); do not create a second workflow from this template.

When using the context optimizer independently, preserve the project's existing rules and edit only the routing that needs improvement. Add concrete routes to existing project authorities if justified.

Do not copy this guidance into the target. Add only links to existing authorities and omit sections that provide no project-specific value. Do not presume a docs index, task snapshot, or handoff file exists.
