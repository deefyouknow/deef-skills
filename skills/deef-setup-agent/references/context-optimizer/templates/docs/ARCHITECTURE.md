# Architecture

## System Overview
Describe the main components and their responsibilities.

## Boundaries
- `<component>` owns `<responsibility>`
- `<component>` must not `<constraint>`

## Important Data / Control Flows
1. `<flow>`
2. `<flow>`

## Non-Obvious Constraints
- `<constraint>`
- `<constraint>`

## Where to Look First
- For `<type of task>` → `<path/doc>`
